2 times 2 times print times_count 1
was wrong

2 times ( 2 times print times_count 1 )
3 times ( 2 times print times_count 2 )



# case 1 :
3 times 2 times print times_count 2 + times_count 1

# case 2 :
3 times 2 times print "" + times_count 2 + times_count 1


def hello-world#0 print 7
hello-world

3 times 2 times print times-count 2 + "," + times-count 1

-----------------------------------------------

I’ll read your current notes file directly and map each block to what behavior it’s testing.

Read [](file:///home/arkenidar/pangea-js/documentation/adjustments.txt)

Yes. I read adjustments.txt, and it makes sense as a compact manual regression checklist.

What each section means:

1. Nested times regression
- 2 times 2 times print times_count 1
- Marked was wrong, so this is the original failing shape.
- The next two parenthesized variants are control cases to compare expected loop-body behavior.

2. atomicArgs check for times_count
- case 1: 3 times 2 times print times_count 2 + times_count 1
- Verifies times_count arguments stay atomic and do not greedily absorb + expressions.

3. Chain decoration/parsing with string + calls
- case 2: 3 times 2 times print "" + times_count 2 + times_count 1
- Verifies left-to-right chain grouping and step markers with mixed string/function operands.

4. Kebab-case normalization for user defs
- def hello-world#0 print 7
- hello-world
- Confirms user-defined kebab names resolve correctly through normalization.

5. Kebab-case normalization in chain expressions
- 3 times 2 times print times-count 2 + "," + times-count 1
- Confirms dash-to-underscore mapping also works in chained expressions and with commas.

So yes, this file is coherent and useful. It is effectively your live smoke-test script for parser/runtime/decoration sync.